import { ReviewTimePipe } from './review-time.pipe';

describe('ReviewTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewTimePipe();
    expect(pipe).toBeTruthy();
  });
});
